The files in this directory are a slight modification of those from:
https://github.com/sky-ecosystem/dss/tree/master/src

Specifically:

1. Zero initializers were added to two `coin` variables.
2. Uses of `now` were replaced with `blockchain.timestamp`.

Sam Moelius (2025-12-26)
